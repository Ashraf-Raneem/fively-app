import tw from "twrnc";
import { background } from "./colors";

export const globalStyle = tw`flex-1 font-[Inter] bg-[${background}]`;
export const globalPublicStyles = tw`flex-1 font-[Inter] bg-[${background}] mt-8 pt-8`;
